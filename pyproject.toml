[project]
name = "aagaaz"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "supabase",
    "python-dotenv",
    "python-multipart"
]

[tool.vercel]
entrypoint = "backend.app.main:app"